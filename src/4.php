 <?php
// Define an array with some animals
$animals = ['Lion', 'Tiger', 'Elephant'];

// Define an array with some colors
$colors = ['Red', 'Green', 'Blue'];

// Create a new array that combines the two arrays
$animalsAndColors = array_merge($animals, $colors);

// Print the combined array
print_r($animalsAndColors);
?>